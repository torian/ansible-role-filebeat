---

- name: Include OS Family vars
  include_vars: "{{ansible_os_family}}.yml"
  tags: always

- include: install.yml
  sudo: true
  tags: fbeat_install

- include: config.yml
  sudo: true
  tags: fbeat_config

# vi:ts=2:sw=2:et:ft=yaml
